{
  "querySql":"SELECT l.returnflag as address, sum(l.quantity) AS sum_qty FROM  tpch.sf1.lineitem AS l WHERE 1=1 GROUP BY l.returnflag",
  "outFileFormat":"csv",
  "catalog": "tpch",
  "schema": "sf1"
}